@model LogoutViewModel

<div class="row">
    <div class="col-sm-6 offset-sm-3">
        <div class="card">
            <div class="card-header">用户注销</div>
            <div class="card-body">
                <form method="post">
                    <input type="hidden" asp-for="LogoutId" />
                    <fieldset>
                        <button class="btn btn-primary">确 定</button>
                    </fieldset>
                </form>
            </div>
        </div>
    </div>
</div>